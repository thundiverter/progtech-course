<h2>Список дисциплин<button class="primary" onclick="toggleDialog()">+ Добавить</button></h2>
<div class="card warning" id="errorExists"><p>Такая дисциплина уже существует</p></div>
<table>
    <tr>
        <th>№</th>
        <th style="width: 100%;">Название дисциплины</th>
        <th></th>
    </tr>

    <% for (let subject of data) { %>
    <tr>
        <td><span class="table-counter"></span></td>
        <td><%= subject.title %></td>
        <td><form action="/subjects/delete" method="post">
            <input type="text" hidden value="<%= subject.id %>" name="id">
            <button type="submit" title="Удалить дисциплину «<%= subject.title %>»">🗑️</button>
        </form></td>
    </tr>
    <% } %>
</table>

<div id="dialog-wrapper">
    <div class="dialog">
        <h3>Добавить дисциплину</h3>
        <form action="/subjects/add" method="post">
            <p>
                <label for="title">Название дисциплины</label>
                <input type="text" name="title" id="title" required>
            </p>
            <button type="submit" class="primary">Сохранить</button>
            <button type="reset" onclick="toggleDialog()">Отмена</button>
        </form>
    </div>
</div>