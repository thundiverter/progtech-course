<h2>Список групп<button class="primary" onclick="toggleDialog()">+ Добавить</button></h2>
<div class="card warning" id="errorExists"><p>Такая группа уже существует</p></div>
<table>
    <tr>
        <th>№</th>
        <th style="width: 100%;">Группа</th>
        <th></th>
    </tr>

    <% for (let subject of data) { %>
    <tr>
        <td><span class="table-counter"></span></td>
        <td><%= subject.title %></td>
        <td><form action="/groups/delete" method="post">
            <input type="text" hidden value="<%= subject.id %>" name="id">
            <button type="submit" title="Удалить группу «<%= subject.title %>»">🗑️</button>
        </form></td>
    </tr>
    <% } %>
</table>

<div id="dialog-wrapper">
    <div class="dialog">
        <h3>Добавить группу</h3>
        <form action="/groups/add" method="post">
            <p>
                <label for="title">Название группы</label>
                <input type="text" name="title" id="title" required>
            </p>
            <button type="submit" class="primary">Сохранить</button>
            <button type="reset" onclick="toggleDialog()">Отмена</button>
        </form>
    </div>
</div>